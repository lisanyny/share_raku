<!DOCTYPE html>
<html>
  <head>
    <title>ShareRaku</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a href="/">シェアらく</a>
          <button class="navbar-toggler" type="button" data-toggler="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggel navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="row">
            <ul>
              <% if current_admin %>
                <li class="nav-item">
                  <%= link_to "予定一覧", admin_events_path, class: 'btn btn-outline-dark' %>
                </li>
                <li class="nav-item">
                  <%= link_to "コメント一覧", admin_comments_path, class: 'btn btn-outline-dark' %>
                </li>
                <li class="nav-item">
                  <%= link_to "会員一覧", admin_customers_path, class: 'btn btn-outline-dark' %>
                </li>
                <li class="nav-item">
                  <%= link_to "ログアウト", destroy_admin_session_path,  method: :delete, class: 'btn btn-outline-dark' %>
                </li>

              <% elsif customer_signed_in? %>
                <li class="nav-item">
                  <%= link_to "Home", homes_top_path, class: 'btn btn-outline-dark' %>
                </li>
                <li class="nav-item">
                  <%= link_to "マイページ", my_page_path, class: 'btn btn-outline-dark' %>
                </li>
                <li class="nav-item">
                  <%= link_to "予定一覧", events_path, class: 'btn btn-outline-dark' %>
                </li>
                <li class="nav-item">
                  <%= link_to "ログアウト", destroy_customer_session_path,  method: :delete, class:'btn btn-outline-dark' %>
                </li>
              <% else %>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
